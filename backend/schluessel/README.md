# Öffentliche Schlüssel der Übergabe-Absender (§ 6 UEBERGABEFORMAT).

In der Ketten-Testinstanz per Compose eingehängt (`docker-compose.integration.yml`).
Nur `.pub` und `.json`, niemals private Schlüssel.
